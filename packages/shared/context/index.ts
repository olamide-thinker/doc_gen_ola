// Shared context providers - to be populated as we extract context
// This will eventually include:
// - AuthProvider / useAuth
// - ThemeProvider / useTheme
// - ProjectProvider / useProject
// etc.

export const sharedContextPlaceholder = true;
